const { expect } = require("chai");

var contractAddress = "";

/*
describe("Deploy the NIPD Officers contract...", function () {
  it("Deploy", async function () {
    const nipd = await ethers.deployContract("NIPDContract");
    
    contractAddress = nipd.target;

    console.log(contractAddress);
  });
});
 */

/* describe("Deploy the NIPD Academy contract...", function () {
  it("Deploy", async function () {
    const nipdoe = await ethers.deployContract("NIPDAcademy");
    
    contractAddress = nipdoe.target;

    console.log(contractAddress);
  });
});
 */